/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "dummyimage.com" /* outros domínios de onde você carrega imagens */,
    ],
  },
};

module.exports = nextConfig;
